# Build output
build/
build-release/
.vs/
*.user

# Dev screenshots / UI-automation artifacts (root + tools only;
# Assets/ images are part of the app and stay tracked)
/*.png
shots/
tools/*.png

# Release zips built locally
/*.zip
